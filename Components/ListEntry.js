import React from 'react';
import { Text, FlatList } from 'react-native';

import Data from '../Data/DummyData';

const ListEntry = () => {
    return (
        <Text>This is a list entry</Text>
    )
};

export default ListEntry;